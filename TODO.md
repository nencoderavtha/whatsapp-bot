# TODO

- [x] Add native Gemini support configuration (AI_PROVIDER=gemini, GEMINI_API_KEY, default model)
- [x] Add Gemini SDK dependency

- [x] Refactor src/ai/agent.ts to use Gemini when selected, while keeping existing tool + runTool workflow

- [x] Verify build/start works with dummy env (no missing credentials crash)

- [ ] Run a quick chat test to confirm tool calls (propose_order/confirm_order/save_customer_info) still function



