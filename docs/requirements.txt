1) llm routings 
2) prompt and response caching
3) multi provider delivery integration
4) automated follow up tasks
5) festival broadcast campaigns
6) production deployment 

-- in the flow 
1) menu carousel
2) when customer name is available <name> andi is not proper sentenct in telugu, ensure proper sentences are framed in language specifc responses. ensure the model does not generate native text like telugu text or hindi text etc, instead it should generatee the language text in english
3) a preview list of previous location or previous order location 
should apper for the user to select any location or custom select the new location
4) s
5) remove location and hours from the welcome message
6) instead of just confirm it should be confirm and pay
7) order summary should neatly split up order cost + delivery cost

--regarding delivery -- 
1) contact delivery agent should be in the status messages 
after rider is scheduled 
2) 

